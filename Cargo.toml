[workspace]
resolver = "2"
members = [
    "crates/hatch-proto",
    "crates/hatch-crypto",
    "crates/hatch-wireguard",
    "crates/hatch-macos",
    "crates/hatch-common",
    "crates/hatch-gpu",
    "crates/hatch-linux",
    "agent",
    "coordinator",
    "ledger",
    "cli",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
authors = ["Hatch Contributors"]
license = "Apache-2.0"

[workspace.dependencies]
# Async runtime
tokio = { version = "1.51", features = ["full"] }
tokio-stream = "0.1"

# Serialization
serde = { version = "1.0.210", features = ["derive"] }
serde_json = "1"
toml = "0.8"
bincode = "2"

# gRPC / protobuf
tonic = { version = "0.12", features = ["tls", "tls-native-roots"] }
prost = "0.13"
prost-types = "0.13"

# HTTP / REST
axum = { version = "0.7", features = ["ws", "macros", "multipart"] }
tower = "0.5"
tower-http = { version = "0.6", features = ["cors", "trace"] }
hyper = "1"
reqwest = { version = "0.12", features = ["json"] }

# P2P networking
libp2p = { version = "0.56", features = [
    "kad",
    "gossipsub",
    "identify",
    "noise",
    "yamux",
    "quic",
    "tcp",
    "relay",
    "dcutr",
    "mdns",
    "macros",
    "tokio",
] }

# NATS messaging
async-nats = "0.38"

# Database
sqlx = { version = "0.8", features = ["runtime-tokio", "sqlite", "postgres", "migrate", "uuid", "time"] }
redis = { version = "0.27", features = ["tokio-comp", "aio"] }

# Crypto
ring = "0.17"
ed25519-dalek = { version = "2", features = ["rand_core", "serde"] }
rand = "0.8"
sha2 = "0.10"
hex = "0.4"

# WireGuard
boringtun = "0.7"

# Networking utils
quinn = "0.11"
rustls = "0.23"

# CLI
clap = { version = "4", features = ["derive", "env"] }
indicatif = "0.17"
console = "0.15"
colored = "2"
dialoguer = "0.11"

# Error handling
anyhow = "1"
thiserror = "2"

# Logging / tracing
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

# UUID / time
uuid = { version = "1", features = ["v4", "serde"] }
time = { version = "0.3", features = ["serde", "formatting", "parsing"] }

# Config
config = "0.14"
dirs = "5"

# System
num_cpus = "1"

# Hashing
blake3 = "1"

# Post-quantum cryptography (NIST FIPS 203/204/205)
pqcrypto-dilithium = { version = "0.5", features = ["serialization"] }
pqcrypto-traits = "0.3"
pqcrypto-kyber = { version = "0.8", features = ["serialization"] }

# Multi-GPU detection
nvml-wrapper = { version = "0.10" }
opencl3 = { version = "0.9" }

# Local crates
hatch-gpu      = { path = "crates/hatch-gpu" }
hatch-proto    = { path = "crates/hatch-proto" }
hatch-crypto   = { path = "crates/hatch-crypto" }
hatch-wireguard = { path = "crates/hatch-wireguard" }
hatch-macos    = { path = "crates/hatch-macos" }
hatch-linux    = { path = "crates/hatch-linux" }
hatch-common   = { path = "crates/hatch-common" }

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = true
