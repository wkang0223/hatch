# NeuralMesh PyTorch MPS Runtime
# PyTorch with Metal Performance Shaders backend for Apple Silicon.
# Install: pip3 install -r requirements.txt

# PyTorch (macOS wheel includes MPS backend)
torch>=2.3.0
torchvision>=0.18.0
torchaudio>=2.3.0

# Standard ML dependencies
numpy>=1.26.0
scipy>=1.13.0
pillow>=10.3.0
tqdm>=4.66.0

# Model loading + tokenizers
transformers>=4.41.0
tokenizers>=0.19.0
sentencepiece>=0.2.0
accelerate>=0.30.0
huggingface-hub>=0.23.0
safetensors>=0.4.3

# Fine-tuning utilities
peft>=0.11.0
bitsandbytes>=0.43.0

# Data
datasets>=2.19.0

# Utilities
einops>=0.7.0
regex>=2024.4.16
requests>=2.31.0
